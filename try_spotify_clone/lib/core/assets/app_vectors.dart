class AppVectors {
  static const basePath = 'assets/vectors/';
  static const format = '.svg';

  static const logo = '${basePath}spotify_logo$format';

  static const lightMode = "${basePath}light$format";

  static const darkMode = "${basePath}dark$format";

  static const topPattern = "${basePath}upper_sign_design$format";

  static const bottomPattern = "${basePath}lower_sign_design$format";
}
